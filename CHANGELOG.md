# 变更记录

本仓库的版本对应 `releases/` 下的整包安装包。

## v1.1.0（2026-09-19）

- 新增统一 `cumcm-flow` CLI、陌生机器 `doctor`、初始化/封存/校验/状态命令和 Windows/PowerShell/Unix 启动器。
- 安装器改为读取包元数据版本，增加 Python 3.11+ 可执行性验证、安装所有权清单、文件哈希检查、事务式暂存/回滚和保守卸载；安装后自动体检。
- 四个核心技能（`数学模型建立`、`数学模型评价`、`cumcm-code-writer`、`cumcm-code-reviewer`）保持 v1.0.2 基线逐文件不变。
- 总指挥收敛为纯编排器：统一独立任务调用，禁止同线程 fallback；新增安全路径、预读封存、严格回执和前置阶段校验。
- 修复交接清单漏封存 preread、S5a/S5c 输入污染、伪造 reproduction、绝对路径越界、符号链接越界和回执状态可绕过等问题。
- 修复 `cumcm-code-reviewer` 缺料：补齐 SKILL.md 引用的报告规范、修复模式库、官方材料、2021–2025 ABC 题面、示例与 `scripts/ocr_pdf.py`。
- 工具链纳入安装：`tools/` 会装到 `<skills>/工具/`，新增 `setup-md2docx.ps1` 与 `requirements-tools.txt`。
- 环境探测改为安装时进行：写入 `工具/环境配置.json` 与 `cumcm-code-writer/环境配置.json`，删除技能中的打包机硬编码路径，`环境配置.json` 不再随包分发。

## v1.0.2（2026-09-19）

- 安全路径、预读封存、严格回执与前置阶段校验；详细修复已并入 v1.1.0 的变更记录。

## v1.0.1（2026-09-17）

- 补齐 `cumcm-code-reviewer` 引用材料；工具链纳入安装；环境探测改为安装时进行。详细修复已并入 v1.1.0 的变更记录。

## v1.0.0（2026-09-17）

- 首个整包版本：6 个技能 + 安装脚本。因旧包内含打包机绝对路径，未纳入本仓库。