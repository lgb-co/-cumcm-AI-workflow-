# CUMCM 全流程工作流 · 安装包 v1.1.0

解压后**双击 `Install.cmd`**（无需管理员权限）。默认优先装到 `%CODEX_HOME%\skills`（若设置了 `CODEX_HOME`），否则使用 `%USERPROFILE%\.codex\skills`（存在则用），再否则使用 `%USERPROFILE%\.agents\skills`；也可用命令行：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Destination "某目录\skills"   # 自定义安装位置
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Action Check                  # 校验安装完整性
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Action Doctor                 # 陌生机器体检
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Action Doctor -Strict         # 严格体检
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Action Uninstall              # 卸载（旧版与本次文件移入备份目录）
powershell -ExecutionPolicy Bypass -File .\install.ps1 -SetupTools                    # 顺带建 DOCX 导出环境（首次需联网）
```

## 包含的技能（各自独立完整文件夹，互不混合）

| 文件夹 | 角色 |
|---|---|
| `数学模型建立` | 建模：拆题、数学化、检索取数，出方案与数据文档 |
| `数学模型评价` | 方案评审：逐问四遍复读 + 联网查证，出诊断与修改方案 |
| `cumcm-code-writer` | 代码编写：按方案写竞赛可读代码，三轮自测，出附录与结果文档 |
| `cumcm-code-reviewer` | 代码检测：干净副本复跑 + 独立复算，出 P0/P1/P2 清单 |
| `cumcm-paper-writer` | 论文成文：起草/改写/校验（格式+风格+数字+赛题逐条对照） |
| `cumcm-fullflow` | 总指挥流程：生成密封清单、调用独立任务、守门禁、核验回执、串起全流程（不自己产出专业内容） |

## 怎么用

在 Codex 新任务里说：**「用 $cumcm-fullflow 跑 2026A：赛题和附件在 <路径>」**。
总指挥会按顺序调用**彼此独立的新任务**：建模 → 方案评审 → 代码实现 → 代码检测 → **结果驱动的模型复盘** → 论文起草/改写/校验 → 交付核验；每个阶段都用密封清单（SHA-256）交接、写结构化回执、过门禁（数据确认/模型定稿会停下等你拍板）。独立任务不可用时会停下或改派，不会由总指挥在当前线程代做。

只评论文/只写代码/只建模等单项任务，不必用总指挥，直接调用对应技能即可。

## CLI 是什么

CLI（Command-Line Interface，命令行界面）是安装包提供的统一工程入口，不是第五个 Skill。它只负责把已有脚本变成可发现、可诊断的命令：

```text
工具\cumcm-flow.cmd doctor                 # 先做陌生机器体检
工具\cumcm-flow.cmd doctor --json
工具\cumcm-flow.cmd init <运行目录>        # 建立运行目录骨架
工具\cumcm-flow.cmd seal S1 --run-dir <运行目录>
工具\cumcm-flow.cmd verify S1 --run-dir <运行目录>
工具\cumcm-flow.cmd status <运行目录>
```

`doctor` 默认把 Python/Skill/工具缺失视为硬失败，把可选计算和文档依赖列为警告；`--strict` 会把可选计算、文档和 OCR 环境缺失也视为失败，`--network` 会额外检查 PyPI。CLI 不替代总指挥，不产出模型、代码、评审或论文。

## 环境与依赖

- 安装脚本会**自动探测本机 Python**（顺序：`CUMCM_CODE_ENV` → `CUMCM_PYTHON` → `MODELING_PY` → PATH 里的 python → Codex 运行时自带 Python），并把结果写入 `<skills>\工具\环境配置.json` 与 `<skills>\cumcm-code-writer\环境配置.json`；换机器重跑安装脚本即可，不需要手改技能里的代码或路径。
- Python 3.11+ 是写码/计算与 DOCX 导出的硬需求；建议同时具备 numpy / pandas / matplotlib / scipy / scikit-learn。
- **查看版 DOCX（Word 原生公式）**需要 pypandoc：首次使用前运行 `工具\setup-md2docx.ps1`，或安装时加 `-SetupTools`。缺依赖时工具会给出提示，不会静默退化成公式图片。
- 可选联网：文献核验、数据抓取、mermaid/kroki 出图。

## 安装内容

- 6 个技能目录（四个核心执行 Skill、论文 Skill、总指挥；装到 `<skills>\<技能名>\`）。
- `BUILD_METADATA.json`：版本、协议号、构建时间和各 Skill 的 `SKILL.md` 哈希；安装前会核验技能目录与哈希。四个核心 Skill 与 v1.0.2 逐文件比较，唯一的元数据例外是刷新 `数学模型评价/manifest.json` 中过期的 `SKILL.md` 校验值。
- `工具\`：`cumcm-flow` CLI 与四个启动/交接脚本、DOCX 导出工具、`BUILD_METADATA.json`；安装时另生成 `环境配置.json`、`python.path` 和所有权清单。
- 覆盖安装/卸载时，被替换的文件会移到 `%USERPROFILE%\.codex\.cumcm-workflow-backups\<时间戳>\`，不会直接删除。

## 变更记录

- **v1.1.0**（2026-09-19）
  - 新增统一 `cumcm-flow` CLI、陌生机器 `doctor`、初始化/封存/校验/状态命令和 Windows/PowerShell/Unix 启动器。
  - 安装器改为读取包元数据版本，增加 Python 3.11+ 可执行性验证、安装所有权清单、文件哈希检查、事务式暂存/回滚和保守卸载；安装后自动体检。
  - 四个核心 Skill 保持不变；总指挥仍只负责编排、调用、交接、门禁和状态。
  - 总指挥收敛为纯编排器：统一独立任务调用，禁止同线程 fallback；新增安全路径、预读封存、严格回执和前置阶段校验。
  - 修复交接清单漏封存 preread、S5a/S5c 输入污染、伪造 reproduction、绝对路径越界、符号链接越界和回执状态可绕过等问题。
  - 修复 `cumcm-code-reviewer` 缺料：补齐 SKILL.md 引用的 `references/报告写作风格_自然专业.md`、`改进建议报告规范.md`、`评审维度与报告结构_全面版.md`、`修复模式库.md`、`references/official/`（3 份）、`references/problem_texts/`（2021–2025 ABC）、`references/examples/`（3 份）与 `scripts/ocr_pdf.py`。
  - 工具链纳入安装：`tools\` 会装到 `<skills>\工具\`，与技能内引用的 `工具\md2docx_native.py` 路径一致；新增 `setup-md2docx.ps1` 与 `requirements-tools.txt`（pypandoc_binary / python-docx / lxml），两个导出脚本缺依赖时给可操作提示而不是裸 traceback。
  - 环境探测改为安装时进行：写入 `工具\环境配置.json` 与 `cumcm-code-writer\环境配置.json`；删除技能中打包者机器的硬编码路径（`数学模型建立` 解释器顺序、`check_docx_toolchain.py` 候选），`环境配置.json` 不再随包分发；修正 `数学模型建立` 中指向不存在的 `数学模型评价/scripts/ocr_pdf.py` 的 OCR 引用。
- **v1.0.2**：总指挥安全路径、预读封存、严格回执、前置阶段校验和工具链纳入安装包。
- **v1.0.0**：首个整包版本（6 技能 + 安装脚本）。
