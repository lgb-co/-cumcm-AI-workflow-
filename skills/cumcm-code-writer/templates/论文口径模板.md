# 论文口径声明 · <项目名>

> 用途：把论文里写下的关键数值逐条登记在此，供 `scripts/check_paper_consistency.py` 与代码结果自动比对。
> 国赛资格红线要求"代码结果与论文一致"：这里对不上就必须改到一致（改代码或改论文），不能靠"作者机器上跑过"解释。

## 声明表

| 指标 | 论文数值 | 来源表 | 容差 |
|---|---|---|---|
| <指标名，尽量与结果表列名一致> | 1350.0 | q2_最优方案.csv | 1e-6 |
| <指标名> | 0.7879 | q1_评价结果.csv | |

## 使用说明

- 「指标」按结果表的列名或首列行标签匹配：完全一致优先，其次取最长的子串匹配。
- 「来源表」写 `output/<项目名>/tables/` 下的文件名，可只写文件名。
- 「容差」留空默认按 1e-6 的相对容差比较；论文里四舍五入过的数值要按精度放宽。
- 论文还没定稿时可先不建本文件：`check_gates.py` 会标注"未做论文数值核对"，但必须在结果文档的限制说明里写明。
